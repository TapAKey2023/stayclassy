<div class="container">
    <div class="row d-flex justify-content-center">
          <h1 class="text-center pt-2"><%= @lesson.name %></h1>
        <div class="col-md-4 mx-auto d-flex flex-column justify-content-center align-items-center">
          <div class="container margin-top-negative d-flex justify-content-center">

            <div class="user-card-image row d-lg-inline-flex" style="width: 18rem;">
              <div class="card d-flex pt-4" style="width: 18rem;">
                <%= cl_image_tag @lesson.photo.key %>
                <div>
                  <p>Name: <%= @lesson.name %></p>
                  <p>Description: <%= @lesson.description %></p>
                  <p>Category: <%= @lesson.category %></p>
                  <p>Total price: £<%= @lesson.total_price %></p>
                  <p>Duration(hour) : <%= @lesson.duration %></p>
                </div>
              </div>
            </div>

          </div>

          <p class="pt-2"> Taught by <%= @lesson.user.name%></p>

            <% if current_user != @lesson.user %>
            <%= link_to "Book this class", new_lesson_booking_path(@lesson), class:"button" %>
            <% end %>

        </div>

            <% if current_user == @lesson.user %>
            <%= link_to "edit this lesson", edit_lesson_path(@lesson)%>
            <%= link_to "delete this lesson", lesson_path(@lesson), class: "btn btn-cancel", data: { turbo_method: :delete, turbo_confirm: "Are you sure you want to delete this class?" } %>
            <% end %>

    </div>
</div>


<div class="container margin-top-negative">
  <h3> Other Classes you may like...</h3>
  <div class="card-grid ">
      <% @other_lessons.each do |lesson| %>
     <div class="card">
         <%= link_to lesson_path(lesson), class:"text-decoration-none card-class" do %>
         <%= cl_image_tag lesson.photo.key %>

             <div class="card-class-infos">
               <h3><%= lesson.name %></h3>
             </div>
         <% end %>
     </div>
     <% end %>
 </div>
<%# ccc %>
